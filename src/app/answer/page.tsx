import { StubPage } from '@/components/ui/StubPage';

export default function AnswerStub() {
  return (
    <StubPage
      title="文字回答页"
      subtitle="伙伴提问 + 文字输入 + 字数限制（300 字）。"
      plannedPhase="P2 (复用主页任务卡浮层；本路由可能在 P2 末期合并掉)"
      prdRef="§10.1 页面清单 / §11.3 Day 4"
    />
  );
}
